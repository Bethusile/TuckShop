{
  "compilerOptions": {
    // File Layout
    "rootDir": "src",
    "outDir": "dist",

    // Environment Settings (Crucial for ESM in Node.js)
    "module": "NodeNext",          // Correct for modern Express/Node.js
    "moduleResolution": "NodeNext",// <--- ADD THIS
    "target": "es2020",
    
    // Type Definitions
    "types": ["node", "express", "pg", "cors"], // <--- ADD Express/PG/CORS types if they were missing
    "esModuleInterop": true,       // Required for compatibility with many packages

    // Other Outputs
    "sourceMap": true,
    // "declaration": true,       // Not strictly needed for a backend app
    // "declarationMap": true,

    // Stricter Typechecking Options
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    
    // Style and Safety Options
    "strict": true,
    "skipLibCheck": true,
    // Remove "verbatimModuleSyntax": true, or leave it if you prefer strictness
    // Remove "isolatedModules": true, (unless you specifically need it for isolated builds)
  }
}